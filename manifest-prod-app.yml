---
applications:
- name: community-prod
  memory: 512M
  instances: 1
  host: community
  domains:
  - cloud.gov
  command: bundle exec puma -t 5:5 -p ${PORT:-3000} -e ${RACK_ENV:-development} --preload
services:
  - community-redis-swarm
  - community-db
  - discourse-env
